#!/usr/bin/env bash
# TODO(ticket audit-failover): turn this stub into the real script.
# Expected flags: --from <replica> --to <replica> --confirm
set -euo pipefail
echo "pg_failover stub: from=$1 to=$2"
