# cs-homework-dsa

Homework submissions for CS 2341 — Data Structures & Algorithms.

Each assignment lives in its own module under `src/`.

## Build

```
cargo build
cargo test
```

## Benchmarks

```
cargo run --release --bin bench > target/bench.json
```

Then open `benchmarks.py` in marimo to visualize.
